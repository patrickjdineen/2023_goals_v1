{
  "name": "goals-23",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "If-env TYPE=server && (cd server && npm isntall) || cd client && npm install",
    "server" : "node ./server/server.js",
    "client" : "cd client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
